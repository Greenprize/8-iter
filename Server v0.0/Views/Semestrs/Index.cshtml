@model IEnumerable<Server_v0._0.Models.Semestr>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Sem)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id_Sem">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Sem">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
