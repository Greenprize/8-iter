@model IEnumerable<Server_v0._0.Models.GradsOfSemestr>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Grade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time.TimeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GradsOfSemestrId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GradsOfSemestrId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GradsOfSemestrId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
